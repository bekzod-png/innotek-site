{
  "name": "innotek-invest-site",
  "version": "1.0.0",
  "private": true,
  "description": "INNOTEK INVEST corporate website — Node.js (built-in modules only, no npm install required)",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
